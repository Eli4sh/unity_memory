using Core;namespace Memory.Logic{    public static partial class Game    {        public static void CreateCards(int pairCount)        {            for (int i = 0; i < pairCount; i++)            {                //TODO: SET CARD PREFAB PATH                var tempObj = Factory.CreateGameObjectFromResources(path: " ");                                //TODO: INIT MEMORY CARD                var card = tempObj.GetComponent<UnityEngine.GameObject>();                //var card = tempObj.GetComponent<MemoryCard>();                SetCardIndex(instanceId: card.GetInstanceID(), i);            }        }    }}