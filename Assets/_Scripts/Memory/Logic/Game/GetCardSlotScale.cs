using Memory.Database;using UnityEngine;namespace Memory.Logic{    public static partial class Game    {        private static Vector3 GetCardSlotScale(int index)        {            return Core.Database.GetDatabaseVector3(key: DatabaseKeys.CardSlotScaleDatabaseKey(index: index)) ?? Vector3.one;        }    }}