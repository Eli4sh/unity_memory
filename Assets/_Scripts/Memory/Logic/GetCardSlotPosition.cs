using Memory.Database;using UnityEngine;namespace Memory.Logic{    public static partial class Game    {        private static Vector3 GetCardSlotPosition(int index)        {            return Core.Database.GetDatabaseVector3(key: DatabaseKeys.CardSlotPositionDatabaseKey(index: index)) ?? Vector3.zero;        }    }}