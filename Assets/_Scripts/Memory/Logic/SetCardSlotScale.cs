using Memory.Database;using UnityEngine;namespace Memory.Logic{    public static partial class Game    {        private static void SetCardSlotScale(int index, Vector3 value)        {            Core.Database.SetDatabaseVector3(key: DatabaseKeys.CardSlotScaleDatabaseKey(index: index), value: value);        }    }}